

const MyBooks = (token) => {
    //Render users books if they have a token ---> if they don't have token render page saying they need to login
        // add API call to index.jsx
        // ** Can probably reuse some of my code from book/book card to create shelf and bookcard for user's books

    //Each book needs a button that handles book return onClick

    // probably need to update state if they return a book, not sure 
    


    return(
        <h2>My Books 📚</h2>

        
    );
}

export default MyBooks